Start a virtual machine.

## Example

```powershell
VBoxManage startvm "zaldua1zerb1"
```

## Command Options

```powershell
VBoxManage startvm [--putenv=name[=value]] [--type=<gui|headless|sdl|separate>] [--password=file]
      [--password-id=password-identifier] <uuid | vmname...>
```